{
  "name": "@billing/shared-types",
  "version": "0.2.0",
  "private": true,
  "description": "Hand-written DB column contracts shared between PG (Super Admin) and SQLite (billing app)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "files": [
    "src"
  ],
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
